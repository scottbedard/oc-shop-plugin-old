# ===================================
#  Form Field Definitions
# ===================================

fields:

    calculator:
        label: bedard.shop::lang.settings.shipping.calculator
        placeholder: bedard.shop::lang.settings.shipping.calculator_placeholder
        type: dropdown
        span: left

    is_required:
        label: bedard.shop::lang.settings.shipping.is_required
        type: dropdown
        options:
            0: bedard.shop::lang.settings.shipping.is_required_off
            1: bedard.shop::lang.settings.shipping.is_required_on
        default: 0
        span: right
