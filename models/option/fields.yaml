# ===================================
#  Form Field Definitions
# ===================================

fields:

    name:
        label: bedard.shop::lang.common.name

    placeholder:
        label: bedard.shop::lang.options.placeholder

    values:
        label: bedard.shop::lang.options.values
        type: valuemanager
        required: true
